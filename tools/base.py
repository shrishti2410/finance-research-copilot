"""Shared tool contract.

Intended contents:
- base type/protocol for a tool: name, description, input schema, run()
- a registry the agent reads to build its tool list
- common error/result envelope
"""
