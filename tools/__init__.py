"""Live capabilities the agent can call.

Each tool is a discrete function with a typed input/output contract and no
knowledge of the agent or the RAG layer. Registered so the agent can select
among them.
"""
