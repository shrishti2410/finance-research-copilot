"""Evaluation runner.

Intended contents:
- iterate a dataset, call the agent, apply metrics, aggregate
- write a report (JSON + summary table); compare against a baseline
- CLI entrypoint for local runs and CI
"""
